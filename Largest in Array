#include<stdio.h>

void main()
{
  int a[50],i,max,n;
  scanf("%d",&n); //number of array elements
  scanf("%d",&a[0]);
  max=a[0];
  for(i=1;i<n;i++)
  {
    scanf("%d",&a[i]);
    if(a[i]>max)
      max=a[i];
  }
  printf("%d",max); //largest element in array
  }
